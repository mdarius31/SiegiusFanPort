package
{
   public class DmgNumber
   {
      
      public var cnt:uint;
      
      public var typ:String;
      
      public var x:Number;
      
      public var y:Number;
      
      public var dmg:uint;
      
      public function DmgNumber(param1:uint, param2:String)
      {
         if(true)
         {
            super();
            if(true)
            {
               dmg = param1;
               if(true)
               {
                  x = Math.random() * 10 - 5;
                  y = Math.random() * 10 - 5;
               }
               cnt = 0;
            }
            typ = param2;
         }
      }
   }
}

